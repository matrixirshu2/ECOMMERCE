'use client';
import { useState } from 'react';
import { LockKeyhole, Mail, UserPlus } from 'lucide-react';
export default function Login(){const [mode,setMode]=useState<'login'|'register'>('login');const [message,setMessage]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();setMessage(mode==='login'?'Demo account session created. Connect your auth provider for production authentication.':'Account created locally. Connect your auth provider for production authentication.')};return <main className="auth-page"><div className="auth-card"><a className="logo" href="/">ECOMMERCE</a><div className="tabs"><button className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Login</button><button className={mode==='register'?'active':''} onClick={()=>setMode('register')}>Create account</button></div><h1>{mode==='login'?'Welcome back':'Create your account'}</h1><form onSubmit={submit}>{mode==='register'&&<label>Name<input required placeholder="Your name" /></label>}<label><Mail size={16}/> Email<input required type="email" placeholder="you@example.com" /></label><label><LockKeyhole size={16}/> Password<input required type="password" placeholder="••••••••" /></label><button className="checkout" type="submit">{mode==='login'?'Login':'Create account'}</button></form>{message&&<p className="notice">{message}</p>}<p className="switch">{mode==='login'?'New here?':'Already have an account?'} <button onClick={()=>setMode(mode==='login'?'register':'login')}><UserPlus size={14}/> {mode==='login'?'Create account':'Login'}</button></p></div></main>}
